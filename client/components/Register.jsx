// CHILD OF NAV

import React from 'react'
import { HashRouter as Router, Route, Link } from 'react-router-dom'
import { connect } from 'react-redux'

const Register = () => (
    <React.Fragment>

{/* Form with log in credentials, auth, and everything else
needed to allow someone to create a brand new provider log in.

Will redirect to the admin page once logged in  */}

    </React.Fragment>
)

export default Register