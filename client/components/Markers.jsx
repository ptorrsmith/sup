import React, { Component, Fragment } from 'react'
import Map from './Map'

// const Markers = [number, number]

//  Props = {
//   content: string,
//   position: Markers,
// }

//  MarkerData = { ...Props, key: string }

// const MyMarker = ({ content, position }: Props) => (
//   <Marker position={position}>
//     <Popup>{content}</Popup>
//   </Marker>
// )

// const MyMarkersList = ({ markers }: { markers: Array<MarkerData> }) => {
//   const items = markers.map(({ key, ...props }) => (
//     <MyMarker key={key} {...props} />
//   ))
//   return <Fragment>{items}</Fragment>
// }

//  State = {
//   markers: Array<MarkerData>,
// }

// export default class CustomComponent extends Component<{}, State> {
//   state = {
//     markers: [
//       { key: 'marker1', position: [ , ], content: '#' },
//       { key: 'marker2', position: [ , ], content: '#' },
//       { key: 'marker3', position: [ , ], content: '#' },
//     ],
//   }

//   render() {
//     return (
//         <Map/>
//         <MyMarkersList markers={this.state.markers} />
//     )
//   }
// }

export default Markers