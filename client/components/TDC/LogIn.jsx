// CHILD OF NAV

import React from 'react'
import { HashRouter as Router, Route, Link } from 'react-router-dom'
import { connect } from 'react-redux'

const LogIn = () => (
    <React.Fragment>

        {/* Form with log in credentials, auth, and everything else
needed to allow someone to log in to use the admin page

Will redirect to the admin page once logged in */}

    </React.Fragment>
)

export default LogIn